<Page x:Class="AgenceLocation.DateRangeSelector"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AgenceLocation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DateRangeSelector">

    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <Label>Date de début :</Label>
            <DatePicker Name="StartDatePicker" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
            <Label>Date de fin :</Label>
            <DatePicker Name="EndDatePicker" SelectedDateChanged="DatePicker_SelectedDateChanged"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="CancelButton" Width="200" Margin="10" Click="CancelButton_Click">Annuler</Button>
            <Button Name="ConfirmButton" Width="200" Margin="10" Click="ConfirmButton_Click" IsEnabled="false">Confirmer</Button>
        </StackPanel>
    </StackPanel>
</Page>
